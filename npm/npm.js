//node modules folder under npm folder stores all dependies of pkg --for ex it will store all code and dependencies of chalk folder.

const chalk = require('chalk');
const validator = require('validator');

// console.log(chalk.green.underline.inverse("Hello World"));
// console.log(chalk.red.underline.inverse("False"));

// const res= validator.isEmail("faiz@0581gmail.com");
// console.log( res?chalk.green.inverse(res):  chalk.red.underline(res));

// const res1= validator.isMobilePhone("+91921918643600");
// console.log( res1?chalk.green.inverse(res1):  chalk.red.underline(res1));